// Backwards-compatible entry point. The canonical generator now keeps the
// complete approved golden Yorks seal across every platform.
import 'prepare_branding.dart' as branding;

void main() => branding.main(const []);
